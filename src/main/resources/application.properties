spring.application.name=keiro-backend

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/keiro_db
spring.datasource.username=postgres
spring.datasource.password=Luisito213
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# App
server.port=8080

# JWT
jwt.secret=keiro_super_secret_key_2024_muy_larga_para_que_sea_segura
jwt.expiration=86400000
